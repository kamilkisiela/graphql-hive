type Query {
  foo: Int!
  bar: String
  test: String
  last: Boolean
  fooTwo: Int!
  foo4: Int!
}

type Mutation {
  addFooT(foo: Int!): Int!
  addBar(bar: String): String
  addTest(test: String): String
}

schema {
  query: Query
  mutation: Mutation
}
# test 3
