query targetActivities($selector: TargetActivitiesSelector!) {
  targetActivities(selector: $selector) {
    nodes {
      __typename
      id
      createdAt
      ...ActivityNode
    }
    total
  }
}
