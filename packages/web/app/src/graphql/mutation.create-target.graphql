mutation createTarget($input: CreateTargetInput!) {
  createTarget(input: $input) {
    selector {
      organization
      project
      target
    }
    createdTarget {
      ...TargetFields
    }
  }
}
