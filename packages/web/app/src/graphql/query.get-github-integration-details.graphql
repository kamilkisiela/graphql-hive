query getGitHubIntegrationDetails($selector: OrganizationSelectorInput!) {
  hasGitHubIntegration(selector: $selector)
  gitHubIntegration(selector: $selector) {
    repositories {
      nameWithOwner
    }
  }
}
