mutation deleteTarget($selector: TargetSelectorInput!) {
  deleteTarget(selector: $selector) {
    selector {
      organization
      project
      target
    }
    deletedTarget {
      __typename
      id
    }
  }
}
