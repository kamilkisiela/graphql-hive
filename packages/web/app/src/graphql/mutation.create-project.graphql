mutation createProject($input: CreateProjectInput!) {
  createProject(input: $input) {
    selector {
      organization
      project
    }
    createdProject {
      ...ProjectFields
    }
    createdTarget {
      ...TargetFields
    }
  }
}
