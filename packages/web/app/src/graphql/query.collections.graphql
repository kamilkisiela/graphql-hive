query Collections($targetId: ID!) {
  collections(targetId: $targetId) {
    nodes {
      id
      name
      items(first: 100) {
        edges {
          node {
            id
            name
          }
        }
      }
    }
  }
}
