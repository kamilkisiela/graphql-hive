mutation createOrganization($input: CreateOrganizationInput!) {
  createOrganization(input: $input) {
    selector {
      organization
    }
    organization {
      ...OrganizationFields
    }
  }
}
