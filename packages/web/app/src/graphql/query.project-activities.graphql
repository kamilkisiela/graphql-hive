query projectActivities($selector: ProjectActivitiesSelector!) {
  projectActivities(selector: $selector) {
    nodes {
      __typename
      id
      createdAt
      ...ActivityNode
    }
    total
  }
}
