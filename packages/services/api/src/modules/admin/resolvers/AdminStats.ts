import type { AdminStatsResolvers } from './../../../__generated__/types.next';

export const AdminStats: AdminStatsResolvers = {
  organizations: async (_parent, _arg, _ctx) => {
    /* AdminStats.organizations resolver is required because AdminStats.organizations exists but AdminStatsMapper.organizations does not */
  },
};
